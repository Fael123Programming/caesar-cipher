#include <cctype>
#include <cstdlib>
#include <iostream>
#include "view/source/View.cpp"
#include "encoder/encoder.cpp"

using namespace std;

int main() {
    Encoder encoder;
    /*View view;
    view.printHeader();
    string answer = view.menu();
    view.clear_prompt();
    while (answer == "1" || answer == "2") {
        if (answer == "1") {
            cout << "Let's cipher everything..." << endl;
        } else if (answer == "2") {
            cout << "Ok, deciphering..." << endl;
        }
        view.printHeader();
        answer = view.menu();
    }*/
    return 0;
}
